{% extends "base.html.twig" %}

{% block title %}🃏 Deck cards 🃏{% endblock %}

{% block body %}
<h1>Deck of Cards Game </h1>

<p>Here are the cards in the deck 👸🏻 : </p>
<ul>
    <div class="card-container">
        {% for card in cards %}
            <div class="card">{{ card.value|capitalize }}{{ card.suit }}</div>
        {% endfor %}
    </div>
</ul>

{% endblock %}